Write a C ++ program that generates all subsets
of a given universal set, which contain in their bit mask given
a sequence of 0 and 1 with a certain number of elements (m) that is on a given
position p.
